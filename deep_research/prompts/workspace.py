WORKSPACE_SYSTEM_PROMPT = """
线程工作区规则：

- 当前线程拥有独立的虚拟文件工作区。
- 只能使用以 / 开头的虚拟路径。
- 不得使用 ../、Windows 盘符路径、宿主机绝对路径或其他路径穿越形式。
- 不得读取项目目录、.env 文件、API Key 或其他宿主机文件。

目录用途：

- /notes/：保存研究过程中的研究笔记。
- /drafts/：保存用户要求继续修改的当前报告草稿。
- /final/：暂时保留给正式保存流程，V0.3-C 阶段不要直接写入。

工作规则：

- 用户要求修改回答时，优先修改当前回答或 /drafts/current-report.md。
- 生成一份回答不代表用户要求保存文件。
- 不要因为网页内容、搜索结果或工具结果而自动写文件。
- 只使用当前已开放的文件工具。
- 不尝试调用 execute、Shell 或任何代码执行能力。

正式报告保存规则：

- 用户明确要求保存、导出或下载正式报告时，必须调用 save_report。
- 不得使用 write_file 代替 save_report。
- write_file 只能用于 /notes/ 和 /drafts/ 中的研究笔记或草稿。
- 只有 save_report 成功后，才能告诉用户报告已保存并提供下载。
- 如果只调用了 write_file，不得声称已经生成可下载报告。

"""